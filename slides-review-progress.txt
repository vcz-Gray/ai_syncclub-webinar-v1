## Codebase Patterns
- Do not change the slide canvas size; only adjust slide-local layout.
- Image-heavy slides need their own classes and should not inherit generic text-first spacing rules.
- Browser measurement using `.slide.scrollHeight` vs `.slide.clientHeight` is the fastest way to catch projection overflow.
- Keep the local source HTML and `webinar/index.html` synchronized in every iteration.

---

## 2026-03-19 - US-001
- Started review loop for image-heavy slides.
- Focus now:
  - slide 6 left/right alignment
  - slide 9 PRD image height
  - slide 14 comparison density and image stacking
- **Learnings for future iterations:**
  - `slide-side-media` markup alone is not enough; matching CSS must exist or the layout silently collapses to a vertical stack.
---

## 2026-03-19 - US-001
- Completed slide 6 alignment review.
- What changed:
  - Converted slide 6 into a true left/right media layout
  - Aligned the Gemini 3/Prototypes card block with the image container top edge
  - Reduced right-side image sizes so the slide stays inside the fixed canvas
- Files changed:
  - webinar/index.html
  - slides-review-prd.json
  - slides-review-progress.txt
  - .claude/ralph-loop.local.md
- Verification:
  - browser review confirmed no overflow on slide 6
  - rendered image sizes on slide 6 fit within the slide
- **Learnings for future iterations:**
  - Top-line alignment matters more than card size symmetry on presentation slides
  - Image-heavy explanation slides need a dedicated layout class instead of generic demo grids
---

## 2026-03-19 - US-002
- Completed compact media review for slide 9 and slide 14.
- What changed:
  - tightened slide 9 PRD image frame height
  - reduced media-heavy container heights
  - rebuilt slide 14 so the left comparison text is lighter and the right comparison images stack vertically
- Files changed:
  - webinar/index.html
  - slides-review-prd.json
  - slides-review-progress.txt
- Verification:
  - browser review confirmed no overflow on slides 9 and 14
  - slide 9 image now renders within a controlled portrait frame
  - slide 14 uses a true left/right layout with a right-side vertical image stack
- **Learnings for future iterations:**
  - Portrait document screenshots should use a dedicated shorter frame instead of inheriting generic demo dimensions
  - Comparison slides are easier to scan when images become a narrow visual proof column rather than a bottom strip
---

## 2026-03-19 - US-003
- Re-reviewed all image-heavy slides after the focused fixes and confirmed the full 25-slide deck stays inside the fixed canvas.
- Rechecked slides:
  - 4, 6, 7, 9, 11, 13, 14, 16, 18, 20, 21, 23
- Verification:
  - browser audit across all 25 slides reported `overflow=false`
  - slide 6 renders as a true left/right layout
  - slide 9 portrait document frame fits
  - slide 14 renders as left comparison text plus right vertical image proof column
- **Learnings for future iterations:**
  - Fixed-canvas decks need browser measurement after every structural change, not just visual inspection.
  - The remaining quality work is now aesthetic tuning, not overflow correction.
---

## 2026-03-19 - Verification script update
- Updated slides-review-prd.json verification to match 22-slide deck (was 25).
- Deck was consolidated from 25→22 in prior session (removed 3 transition slides: PRD done, User journey intro, TRD intro).
- All stories still pass with updated verification.
---
